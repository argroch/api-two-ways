== APIs: Two Ways

Okay, so I just wanted to try out a couple things:

1. I wanted to try out pulling data from the JSON file that <tt>rails g scaffold</tt> creates. So, using good ol' HTTParty to do that. (That's what the <tt>index</tt> action/view is about.)

2. I'm always pulling JSON with HTTParty. What about XML APIs? And what about Nokogiri? Let's check that out.
